name: "😵 Installation"
description: "Report an installation problem"
labels: installation

body:
  - type: markdown
    attributes:
      value: >
        #### Before submitting an installation problem, please make sure the issue hasn't been already reported by searching through [the existing and past issues](https://github.com/pyg-team/pyg-lib/issues).
        #
  - type: textarea
    attributes:
      label: 😵 Describe the installation problem
      description: |
        Please provide a clear and concise description of the installation problem. If you have installation log files, please prvoide them here as well. It may be relevant to wrap the log files in ```` ```triple quotes blocks``` ````.
      placeholder: |
        A clear and concise description of the installation problem.
    validations:
      required: true
  - type: textarea
    attributes:
      label: Environment
      description: |
        Please provide as much information as possible about your environment, such as your `pyg-lib` (`print(pyg_lib.__version__)`) and PyTorch version (`print(torch.__version__)`), your OS (*e.g.*, Linux), and your Python version (*e.g.*, `3.13`):
      value: |
        * `pyg-lib` version:
        * PyTorch version:
        * OS:
        * Python version:
        * CUDA/cuDNN version:
        * How you installed PyTorch and `pyg-lib` (`conda`, `pip`, source):
        * Any other relevant information:
